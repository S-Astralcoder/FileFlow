[project]
name = "fileflow"
version = "0.1.0"
description = "A Script based file management tool"
readme = "README.md"
authors = [
    { name = "S-Astralcoder", email = "suraj.astralcode.ai@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
fileflow = "fileflow:main"

[build-system]
requires = ["uv_build>=0.11.6,<0.12.0"]
build-backend = "uv_build"
